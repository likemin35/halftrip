# Templates

실제 지역별 숙박확인서 PDF 원본이 아직 제공되지 않아, 현재 MVP는 공통 템플릿 이름만 유지하고 FastAPI에서 placeholder PDF/JSON 구조를 생성합니다.

- `COMMON_LODGING_TEMPLATE_PATH` 환경변수는 향후 실제 PDF 파일 경로 연결용입니다.
- 현재 파일이 없어도 서버는 동작합니다.

